# Sigil dedicated system group + user (#10). Idempotent: a no-op if they exist,
# so it coexists with an org that pre-creates them via config management.
g sigil -
u sigil - "Sigil daemons" /nonexistent /usr/sbin/nologin
